PORT=5000

DB_PORT-port number
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=DB pass
DB_NAME=db_name

JWT_ACCESS_SECRET=access_secret_key

JWT_REFRESH_SECRET=refresh_secret_key

ML_SERVICE_URL=http://localhost:8000

# Optional — without Redis, raw files use in-memory cache (dev only)
REDIS_URL=redis://localhost:6379

